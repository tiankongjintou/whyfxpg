# 核心算法说明书

> 基于 2026-08-05 代码审计

## 一、多维风险评分算法

### 1.1 评分公式

`risk_scorer.py` 中的 `RiskScorer.score()` 方法实现：

```
综合风险分 = SS × PS × country_factor × product_factor × history_factor × evidence_factor × causal_factor
```

其中：
- **SS（严重度分）**：基于 `severity_level` 字段，通过 `severity_to_score()` 查量表得到 0-100 的分数
- **PS（概率分）**：基于 `probability_level` 和 `country_history_count`，通过 `probability_to_score()` 得到 0-100 的分数
- **country_factor（国别系数）**：查 `risk_model.yaml` 中的 `country_factors` 映射表，默认 1.0
- **product_factor（品类系数）**：查 `risk_model.yaml` 中的 `product_factors` 映射表，默认 1.0
- **history_factor（历史系数）**：基于 `event_count_12m`（12个月内同类事件数）计算，公式来自 `risk_model.yaml` 中的 `history_factor.formula`
- **evidence_factor（证据系数）**：基于 `source_id` 查 `evidence_factors` 映射表，默认 0.9
- **causal_factor（因果因子）**：由 `CausalKnowledge.get_causal_factor()` 计算的外部因果增强系数

### 1.2 各维度计算细节

#### 严重度分（SS）

```python
def severity_to_score(self, severity_level: str) -> int:
    # 从 risk_model.yaml 的 severity_levels 映射表中查找
    # 返回 level.score，若无则回退到 level.default
    # 默认键："中等" -> score=60
```

量表定义示例（来自 risk_model.yaml）：
- 严重：score=100
- 一般：score=60
- 轻微：score=30

#### 概率分（PS）

```python
def probability_to_score(self, event, history_count) -> int:
    # 优先使用事件自带的 probability_level
    # 否则基于 history_count 推断：
    #   history_count >= 5  → "非常可能" → 分数 100
    #   history_count >= 2  → "可能"     → 分数 95
    #   history_count >= 1  → "不太可能" → 分数 30
    #   history_count = 0   → "可能"     → 分数 95（默认）
```

#### 国别系数

```python
def country_factor(self, country: str) -> float:
    return self._cfg.country_factors.get(country, 
                                         self._cfg.country_factors.get("unknown", 1.0))
```

#### 品类系数

```python
def product_factor(self, category: str) -> float:
    return self._cfg.product_factors.get(category, 
                                         self._cfg.product_factors.get("unknown", 1.0))
```

#### 历史系数

```python
def history_factor(self, event_count_12m: int) -> float:
    formula = self._cfg.history_factor.formula  # 如 "1 + min(event_count_12m * 0.1, 0.5)"
    value = eval(formula, {"__builtins__": {"min": min, "max": max}}, 
                 {"event_count_12m": event_count_12m})
    return max(min_val, min(value, max_val))  # 夹断到 [min, max] 范围
```

#### 证据系数

```python
def evidence_factor(self, source_id: str) -> float:
    return self._cfg.evidence_factors.get(source_id, 
                                          self._cfg.evidence_factors.get("unknown", 0.9))
```

### 1.3 风险等级映射

```python
def map_to_risk_level(self, total_score: float) -> str:
    if total_score >= thresholds.get("S", 8000):  # ≥8000 → S
        return "S"
    elif total_score >= thresholds.get("M", 3000):  # ≥3000 → M
        return "M"
    elif total_score >= thresholds.get("L", 1000):  # ≥1000 → L
        return "L"
    else:
        return "A"
```

> ⚠️ 注意：阈值在 risk_model.yaml 中配置，当前默认 S≥8000, M≥3000, L≥1000。但需求书中定义的分值区间为 S≥85, M≥70-84, L≥50-69, A<50，代码中的阈值与需求书不一致。

---

## 二、反馈学习引擎算法

### 2.1 学习触发条件

`FeedbackLearner` 类（`feedback_learner.py`）实现：

```python
MIN_REVIEWS_FOR_LEARNING = 5      # 至少5条修正才触发学习
CONSISTENCY_THRESHOLD = 0.70      # 修正一致性阈值（超过半数同向）
CLIPPING_THRESHOLD = 0.05        # 修正幅度阈值（5%以上才调整）
LEARNING_RATE = 0.20             # 每次学习调整20%的差距
```

### 2.2 国别系数学习

```python
def analyze_country_learning(self, grouped) -> List[Dict]:
    for country, reviews in grouped.items():
        if len(reviews) < MIN_REVIEWS_FOR_LEARNING:
            continue
        
        # 统计 rs_level 降级修正比例
        downgrades = sum(1 for r in reviews 
                        if rs_score(adjusted_rs) < rs_score(model_rs))
        consistency = downgrades / len(reviews)
        
        if consistency >= CONSISTENCY_THRESHOLD:
            avg_adjustment = average((adjusted_ss - original_ss) / original_ss)
            if abs(avg_adjustment) >= CLIPPING_THRESHOLD:
                # 计算新系数 = 当前系数 - avg_adjustment × LEARNING_RATE
                new_factor_delta = -avg_adjustment * LEARNING_RATE
```

**学习结果应用到 risk_model.yaml**：
```python
new_val = max(0.5, min(2.0, current + delta))  # 夹断到 [0.5, 2.0]
```

### 2.3 产品类别系数学习

算法与国别学习相同，作用于 `product_factors` 配置项。

### 2.4 制造商因果节点学习

```python
def analyze_manufacturer_learning(self, grouped) -> List[Dict]:
    # 当制造商持续被降级修正时
    # 调整其 causal_nodes 表中的 risk_score
    score_delta = -avg_adj / 100 * LEARNING_RATE  # ss_score约50-100范围
    new_risk_score = max(0.1, min(1.0, current_score + score_delta))
```

---

## 三、规则引擎匹配逻辑

### 3.1 规则编译

`RuleEngine.compile()` 调用 `RuleCompilerPort.compile()`：

```python
def compile(self, rule: AlertRule) -> CompiledRule:
    # 将 AlertRule.condition (Dict) 编译为可执行的 CompiledRule
    # 包含 QueryPlan：操作类型、源表、过滤条件、聚合方式
```

### 3.2 规则条件类型

当前实现的条件类型（来自 `alert_rules.yaml`）：

| condition.type | 说明 |
|---------------|------|
| `risk_level_change` | 风险等级在时间窗口内发生变化 |
| `count_by_dimension` | 某维度在时间窗口内事件数超过阈值 |
| `month_over_month_growth` | 环比增长率超过阈值 |
| `novel_pattern` | 首次出现的新危害类型 |

### 3.3 规则评估

```python
def evaluate(self, compiled: CompiledRule, context: RuleContext) -> RuleOutcome:
    # 根据 QueryPlan 执行查询
    # 返回 matched_rows（匹配的记录列表）和 triggered（是否触发）
```

### 3.4 规则沙箱测试

```python
def sandbox(self, rule: Any, fixture: Any) -> SandboxResult:
    # 在内存数据上测试规则
    # 使用 PandasRuleCompilerAdapter 进行求值
```

---

## 四、因果知识图谱算法

### 4.1 因果因子计算

`DbCausalAdapter.factor()` 实现：

```python
def factor(self, event) -> float:
    # 查找事件制造商对应的 causal_nodes[node_type=manufacturer]
    # 返回该节点的 risk_score 作为因果增强因子
    # 若无节点，返回 1.0（无增强）
```

> ⚠️ 当前实现仅返回节点的 risk_score，无多跳因果链传播计算。

### 4.2 下游风险计算

`CausalReasoning.compute_downstream_risk()` 实现：

```python
def compute_downstream_risk(self, node_id, store) -> float:
    # 从给定节点出发，递归遍历所有下游边
    # 累积计算：total_risk = risk_score + Σ(weight × downstream_risk)
    # 返回归一化后的风险值
```

### 4.3 反事实推理

```python
def counterfactual(self, event, intervention) -> Dict:
    # intervention: {"action": "replace_supplier", "target": "manufacturer:XXX"}
    # 计算假设性干预后的风险变化
    # 返回 {"original_risk": float, "hypothetical_risk": float, "delta": float}
```

---

## 五、与需求书的差距

| 维度 | 需求书定义 | 当前实现 | 差距 |
|------|-----------|---------|------|
| 风险分范围 | 0-100 | 0-10000+（乘法溢出风险） | ⚠️ 乘法可能导致极端值 |
| 等级阈值 | S≥85, M≥70-84, L≥50-69, A<50 | S≥8000, M≥3000, L≥1000, A<1000 | ⚠️ 阈值不匹配需求书 |
| 时效分 | 衰减曲线计算 | ❌ 未实现 | ❌ 缺失 |
| 动态评分刷新 | 新信号触发重算 | ❌ 未实现 | ❌ 缺失 |
| 因果传导 | 多跳传播 | ⚠️ 仅单跳 | ⚠️ 部分实现 |
| 反馈学习 | 贝叶斯更新 | 简单系数调整 | ⚠️ 算法简化 |
| 规则条件 | 自然语言解析 | 仅结构化条件 | ❌ 自然语言未实现 |
